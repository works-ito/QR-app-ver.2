name: Patch master route separation

on:
  push:
    branches:
      - main
    paths:
      - scripts/patch_master_route_separation.py

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Run focused patch
        run: python3 scripts/patch_master_route_separation.py
      - name: Verify diff
        run: |
          git diff --check
          git diff -- app.js index.html
      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app.js index.html
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git commit -m 'Separate master completion route from irregular'
          git push origin HEAD:main
